I have an existing Next.js application in my current workspace. I need to create a .NET 10 Minimal API that will serve as the backend for this specific Next.js app. 

Please do the following:
1. Create a new .NET 10 Minimal API project called Fikrat.Api inside src/ folder
2. Configure CORS in the .NET 10 API to allow requests from the Next.js local development server (usually http://localhost:3000).
3. Set up a few standard placeholder endpoints (e.g., a GET health check, and a sample CRUD resource) using the modern .NET 10 Minimal API syntax.
4. Provide clear instructions or a script on how to run both the Next.js frontend and the .NET 10 backend simultaneously during development.
5. Use Fowler's transaction script pattern BL inside services, avoid primary constructors for bare classes

Please review my current directory structure first, then generate the necessary .NET files and configuration.
